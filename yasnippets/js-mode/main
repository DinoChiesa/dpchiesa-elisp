# key : main
# name : main
# --
function main(args) {
    var i = 0;
    try {
        args.forEach(function(arg){
            $0
        });
    }
    catch (exc1) {
        say("Exception:" + JSON.stringify(exc1));
    }
}

main(process.argv.slice(2));

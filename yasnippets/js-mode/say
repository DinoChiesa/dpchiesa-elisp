# key : say
# name : function say(...)
# --
function say() {
  if (typeof console == 'object') {
      console.log.apply(console, arguments);
  }
  else {
    print((arguments && arguments[0]) || '');
  }
}
